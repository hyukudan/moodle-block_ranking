/**
 * Ranking block AMD module.
 *
 * Provides auto-refresh polling for ranking data and smooth animations.
 *
 * @module     block_ranking/ranking
 * @package    block_ranking
 * @copyright  2024 block_ranking contributors
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("block_ranking/ranking",["jquery","core/ajax","core/notification"],(function($,Ajax,Notification){var POLL_INTERVAL=6e4,pollTimer=null;function stopPolling(){pollTimer&&(clearInterval(pollTimer),pollTimer=null)}function fetchRanking(courseid){Ajax.call([{methodname:"block_ranking_get_ranking",args:{courseids:[courseid]}}])[0].done((function(response){response&&response.leaderboard&&function(leaderboard){var $generalList=$('[data-ranking-type="general"]');if(!$generalList.length||!leaderboard.length)return;var $currentItems=$generalList.find(".ranking-item"),changed=!1;$currentItems.each((function(index){if(index<leaderboard.length){var $item=$(this),newPoints=leaderboard[index].points;parseFloat($item.find(".ranking-points").text())!==newPoints&&(changed=!0,$item.find(".ranking-points").text(newPoints),$item.addClass("ranking-updated"),setTimeout((function(){$item.removeClass("ranking-updated")}),1500))}})),changed&&($generalList.closest(".block_ranking").addClass("ranking-refreshed"),setTimeout((function(){$generalList.closest(".block_ranking").removeClass("ranking-refreshed")}),1e3))}(response.leaderboard)})).fail((function(error){window.console&&window.console.warn&&window.console.warn("block_ranking: ranking poll failed",error),stopPolling()}))}return{init:function(courseid,pollinterval){pollinterval&&(POLL_INTERVAL=pollinterval),courseid&&POLL_INTERVAL>0&&function(courseid){document.addEventListener("visibilitychange",(function(){document.hidden?stopPolling():pollTimer=setInterval((function(){fetchRanking(courseid)}),POLL_INTERVAL)})),pollTimer=setInterval((function(){fetchRanking(courseid)}),POLL_INTERVAL)}(courseid),function(){if("IntersectionObserver"in window){var observer=new IntersectionObserver((function(entries){entries.forEach((function(entry){entry.isIntersecting&&(entry.target.classList.add("ranking-visible"),observer.unobserve(entry.target))}))}),{threshold:.1});document.querySelectorAll(".ranking-item").forEach((function(item){observer.observe(item)}))}}()}}}));

//# sourceMappingURL=ranking.min.js.map